# .env file

# Local anvil: the well-known anvil dev account #0 (public test key — local use only).
PRIVATE_KEY_ANVIL="0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
# Base Sepolia: your own funded key (leave blank for local-only).
PRIVATE_KEY_BASE_SEPOLIA=""

# Seed phrase for the extra test wallets (public test phrase — local use only).
# The owner funds these from its balance during tests.
SEED_PHRASE="garden cage click scene crystal fat message twice rubber club choice cool"

# This should be an RPC URL provided by a proper provider  
# that supports the eth_getLogs() and eth_getFilteredLogs() methods.
LOCAL_CHAIN_RPC_URL="http://localhost:8545"
BASE_SEPOLIA_RPC_URL="https://base-sepolia-rpc.publicnode.com"
